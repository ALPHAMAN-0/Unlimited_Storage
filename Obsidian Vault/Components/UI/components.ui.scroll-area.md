---
tags: [ui-primitive]
---

# scroll-area

`src/components/ui/scroll-area.tsx`

Wraps @base-ui/react/scroll-area into ScrollArea and ScrollBar, providing a custom-styled scrollable viewport (e.g. for the file list or sidebar) with themed scrollbar thumb.

## Depends on

- [[lib.utils]] — Exports the cn() helper that merges Tailwind class names via clsx and tailwind-merge, used throughout UI components for conditional styling.

## Used by

*Not imported elsewhere in `src/`.*
