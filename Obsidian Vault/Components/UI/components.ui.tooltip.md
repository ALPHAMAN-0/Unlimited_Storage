---
tags: [ui-primitive]
---

# tooltip

`src/components/ui/tooltip.tsx`

Wraps @base-ui/react/tooltip into TooltipProvider/Tooltip/Trigger/Content, rendering hover tooltips with a positioned arrow for icon buttons throughout the UI.

## Depends on

- [[lib.utils]] — Exports the cn() helper that merges Tailwind class names via clsx and tailwind-merge, used throughout UI components for conditional styling.

## Used by

*Not imported elsewhere in `src/`.*
