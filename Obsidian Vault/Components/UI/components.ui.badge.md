---
tags: [ui-primitive]
---

# badge

`src/components/ui/badge.tsx`

Exports Badge and badgeVariants (cva-driven default/secondary/destructive/outline/ghost/link styles) as a small pill-shaped label component used for tags/status indicators.

## Depends on

- [[lib.utils]] — Exports the cn() helper that merges Tailwind class names via clsx and tailwind-merge, used throughout UI components for conditional styling.

## Used by

*Not imported elsewhere in `src/`.*
